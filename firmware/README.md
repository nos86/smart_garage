# Firmware

This directory contains the PlatformIO-based firmware project structure.

- `include/` for project headers
- `lib/` for private libraries
- `src/` for application source files
- `test/` for PlatformIO tests
