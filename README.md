# smart_garage
Smart relays for Garage lights

## Project structure

- `hardware/schematic/` for EasyEDA schematic files
- `hardware/pcb/` for EasyEDA PCB design files
- `hardware/gerber/` for generated Gerber outputs
- `firmware/` for the PlatformIO-based firmware project
